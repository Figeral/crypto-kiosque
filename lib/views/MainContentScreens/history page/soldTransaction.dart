import 'package:flutter/widgets.dart';

class SoldTransactions extends StatelessWidget {
  const SoldTransactions({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
