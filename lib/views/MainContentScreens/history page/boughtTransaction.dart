import 'package:flutter/widgets.dart';

class BoughtTransactions extends StatelessWidget {
  const BoughtTransactions({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
